/**
 * Guest Root Layout
 *
 * Simple layout for guest pages (landing, login, register)
 * No sidebar or authentication checks needed
 */
export default function GuestRootLayout({ children }: { children: React.ReactNode }) {
  return children
}
